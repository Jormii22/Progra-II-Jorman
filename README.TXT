PROJECT TITLE: Tarea programada 1

Como iniciar el proyecto: Para ejecutar este proyecto desde Ubuntu: se elige la carpeta desde la terminal en esta se digitara el siguiente 
comando g++ main.cpp Envio.cpp, luego de esto se digitara "./a.out" con esto se ejecutara el programa con los parametros que ya tenia asignados,
los valores a modificar necesarios están en el archivo main.cpp,
ArregloEnvios[] = new envio() dentro del parentesis se agregaran ya sean 2 parametros para fedex o 3 para los envios de tipo postal, estos deberan 
ser modificados si se desea un calculo distinto, en el archivo que esta ya asignado se hacen los calculos para un envio de tipo fedex, al agregar un parametro mas
con un numero entre 1 y 3 se asignara la clase para que asi se ejecute el programa con un envio de tipo postal, en este momento esta asignado para
que se realicen 6 envios del mismo tipo como se menciono anteriormente de tipo fedex.

AUTHORS: Jorman Herrera Valerin Carnet C13776